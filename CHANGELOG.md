- 2026-05-21: Add Market Changes summary — services/market_changes.py, GET /stablecoins/changes endpoint, top-level dashboard section, and tests/test_market_changes.py.
- 2026-05-21: Add stablecoin profile pages — services/profile.py, GET /stablecoins/{symbol}/profile endpoint, new "Asset Profile" tab + clickable overview rows (price/supply/chains/liquidity/scores/reserves/freshness, deep-linkable via ?symbol=), and tests/test_profile.py.
- 2026-05-21: Add system-wide data freshness — services/freshness.py (compute_data_freshness), GET /data-freshness endpoint, and a "Data Freshness" panel in the API Usage tab showing per-source status (fresh/delayed/stale/missing), per-provider request health, and stale/failing warnings. Tests in tests/test_freshness.py.
- 2026-05-21: Add Risk Events Timeline — risk_events table (RiskEvent model + schema), services/risk_events.py detecting PEG_DEVIATION/LIQUIDITY_DROP/SUPPLY_SHOCK/SCORE_CHANGE/RESERVE_STALE/API_FAILURE (idempotent step-change detection, auto-logged from the scoring pipeline), GET /risk-events + GET /stablecoins/{symbol}/events endpoints, a "Risk Events" dashboard tab with asset/type/severity filters, and tests/test_risk_events.py.
- 2026-05-21: Add Job Run History — pipeline_runs table (PipelineRun model + schema), services/pipeline_runs.py (record_run context manager wrapping all five pipelines' run(), plus query_runs/pipeline_status_summary), GET /pipeline-runs endpoint (per-pipeline summary + filterable run log), and a "Pipeline Runs" panel in the API Usage tab (health pills, summary table, failed-pipeline callout, recent-runs expander). Tests in tests/test_pipeline_runs.py.
- 2026-05-21: Add Data Validation Rules — data_quality_warnings table (DataQualityWarning model + schema), services/data_validation.py detecting IMPOSSIBLE_PRICE/NON_POSITIVE_SUPPLY/PEG_DEVIATION_MISMATCH/SUPPLY_JUMP/DUPLICATE_SNAPSHOT/MISSING_CHAIN_DISTRIBUTION with an open/auto-resolve lifecycle (run_validation auto-called from the scoring pipeline, idempotent), GET /data-quality endpoint (active-warning summary + filterable warning list), and a "Data Quality" panel in the API Usage tab (severity pills, callout, severity-sorted table). Tests in tests/test_data_validation.py.
- 2026-05-21: Add Historical Risk Regime Labels — regime_snapshots table (RegimeSnapshot model + schema), services/regimes.py (classify_regime/record_regimes/current_regimes/get_regime_detail; six regimes derived deterministically from score + peg + liquidity + data-quality state, transition-only history), REGIME_CHANGE risk-event type + _detect_regime detector wired into the scoring pipeline, GET /regimes + GET /stablecoins/{symbol}/regime endpoints, and dashboard surfacing (Overview "Regime" column, Asset Profile current-regime callout + Risk Regime History step chart, Regime Change rows in Risk Events). Tests in tests/test_regimes.py.
- 2026-05-21: Add Explainable Score Drilldowns — services/score_explanation.py (explain_scores), GET /stablecoins/{symbol}/score-explanation endpoint, and drilldowns in the Risk Scores tab + Asset Profile breakdown showing per-dimension inputs/weights/point contributions, the weakest dimension (by weighted points lost), and a plain-language delta vs the prior snapshot. Extracted shared SCORE_WEIGHTS constant in pipelines/score_stablecoins.py. Tests in tests/test_score_explanation.py.
- 2026-05-21: Add 24h/7d Liquidity Change — services/liquidity.py (get_liquidity_detail: current depth, 24h/7d total-depth change with severity, bid/ask imbalance + 24h move, trend, history; largest_liquidity_drops cross-asset ranking; window comparison requires ≥half-window of separation so short history yields None not a misleading number), GET /stablecoins/{symbol}/liquidity + GET /stablecoins/liquidity-drops endpoints, enhanced Order Book Liquidity section on the Asset Profile (trend callout + depth-over-time chart) and a "Largest Liquidity Drops" table in the Peg Deviation tab. Tests in tests/test_liquidity.py.
- 2026-05-21: Add Chain Concentration Risk — services/chain_concentration.py (get_chain_concentration: normalized chain rows + top-chain share + HHI + graded concentration_level/severity/warning; chain_concentration_ranking: cross-asset most-concentrated-first ranking, ticker-collision-safe, derived on read from supply_by_chain JSON via the shared profile parser), GET /stablecoins/{symbol}/chain-supply + GET /stablecoins/chain-concentration endpoints, and a "Chain Concentration Risk" section in the Supply tab (highly-concentrated warning callout, asset×chain supply-share heatmap, sortable comparison table). Tests in tests/test_chain_concentration.py.
- 2026-05-22: Add Stablecoin Dominance and Market Share — services/dominance.py (compute_dominance: total tracked supply, per-asset market share, dominant asset, plus 7d/30d market-share-ago + share-change in percentage points, all derived on read from supply_snapshots with ticker-collision collapse; a window's share-ago is only computed from a snapshot ≥half-window old, otherwise None, and the past denominator sums only assets with sufficiently-old history so a newly-tracked coin gets no phantom swing; market_share_movers derives gainers/losers), GET /stablecoins/rankings?window=7d|30d endpoint (registered before /stablecoins/{symbol} so it isn't shadowed; bundles rankings + movers), and a "Market Dominance & Share" section in the Supply tab (dominance KPIs, market-share donut, gainers/losers split, full ranking table). Tests in tests/test_dominance.py.
- 2026-05-22: Add Provider Fallback Status — provider_fallback_events table (ProviderFallbackEvent model + schema), services/provider_fallback.py (get_peg_prices now returns per-symbol provenance; record_fallback_events logs only fallback/unavailable outcomes idempotently; get_fallback_status derives the primary-vs-fallback rate + current source per asset from price_snapshots.source, the reason from the event table, and grades the primary healthy/degraded/failing/unknown), GET /provider-fallback endpoint, a "Provider Fallback" panel in the API Usage tab (health KPI strip, callout, per-asset source table, recent-events expander) and a Coinbase-fallback flag on the Asset Profile price card. Also fixed a real bug: update_prices hard-coded source="binance" even when ingestion fell back to Coinbase, so the snapshot now records the provider that actually served the price. Tests in tests/test_provider_fallback.py.
